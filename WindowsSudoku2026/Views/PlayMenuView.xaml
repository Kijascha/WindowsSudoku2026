<UserControl x:Class="WindowsSudoku2026.Views.PlayMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsSudoku2026.Views" xmlns:viewmodels="clr-namespace:WindowsSudoku2026.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:PlayMenuViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Foreground="{DynamicResource AccentBrush}" FontSize="24" Margin="30" Text="Choose your Puzzle Mode" />
            <Button Width="250" Height="40" Content="Auto Generated Puzzles" IsEnabled="False" Style="{DynamicResource CommonButtonStyle}" FontSize="18" Margin="5" Command="{Binding GoToAutoGeneratedPuzzlesCommand}"/>
            <Button Width="250" Height="40" Content="Custom Puzzles" Style="{DynamicResource CommonButtonStyle}" FontSize="18" Margin="5" Command="{Binding GoToCustomPuzzlesCommand}"/>
            <Button Width="250" Height="40" Content="Back" Style="{DynamicResource CommonButtonStyle}" FontSize="18" Margin="5" Command="{Binding GoBackCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
