<UserControl x:Class="WindowsSudoku2026.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsSudoku2026.Views" 
             xmlns:core="clr-namespace:WindowsSudoku2026.Core"
             xmlns:viewmodels="clr-namespace:WindowsSudoku2026.ViewModels"
             xmlns:converters="clr-namespace:WindowsSudoku2026.Core.Converters;assembly=WindowsSudoku2026.Core"
             d:DataContext="{d:DesignInstance Type=viewmodels:NotificationViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:NotificationTypeToBrushConverter x:Key="NotificationTypeToBrushConverter" />
    </UserControl.Resources>
        <Border Background="{Binding Type, Converter={StaticResource NotificationTypeToBrushConverter}}" 
                            CornerRadius="10" 
                            Padding="20,20" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Top" 
                            Margin="0,15,0,0"
                            MinWidth="300"
                            MaxWidth="600">
            <StackPanel Orientation="Horizontal">
                <!-- Icon basierend auf dem Typ (via Converter oder Trigger) -->
                <TextBlock Text="{Binding Message}" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
            </StackPanel>

            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
        </Border>
</UserControl>
